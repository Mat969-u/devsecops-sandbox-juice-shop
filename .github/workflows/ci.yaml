name: "CI"
on:
  push:
    branches:
      - master
    workflow_dispatch: # Allow manual triggering of workflow
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      # - name: Install dependencies
      #   run: npm install
      # - name: Run tests
      #   run: npm test
      - name: GHA testings
        run: echo "GHA is working"